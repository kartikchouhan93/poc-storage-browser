# Porter Agent Builds

Drop build artifacts here following the naming convention:

```
porter-{version}-linux.deb
porter-{version}-windows.exe
porter-{version}-mac.dmg
```

Update `manifest.json` with the new version and set `available: true` for each build that is ready.
