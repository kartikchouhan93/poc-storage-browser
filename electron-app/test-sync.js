const axios = require('axios');
const fs = require('fs');

async function test() {
    let token = '';
    // Let's try to do a POST without token first, we should get 401. If we can get a token, we can test it.
}
test();
